# Local Database Configuration (Fallback)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=fitaix
DB_PASSWORD=mohit123
DB_PORT=5432

# Remote Database Configuration
# Uncomment the line below and paste your remote PostgreSQL connection string
# DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
